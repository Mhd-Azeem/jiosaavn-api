# Az APK Runner

Lightweight Windows APK runner project.

Goal: run Android APKs on Windows with a minimal GUI and an emulator/runtime backend while keeping the total installed footprint as small as practical.
